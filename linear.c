#include "linear.h"
#include "blash.h"

Linear* linear_init(int in_features, int out_features) {
    Linear* linea = (Linear*)malloc(sizeof(Linear));
    in_features = in_features;
    out_features = out_features;
    weights = (float*)malloc(sizeof(float) * out_features * in_channels);
    
}